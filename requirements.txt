# Direct dependencies for the core model and regression suite.
# Exact pins preserve known numerical behavior; bounded ranges allow compatible
# platform-specific Torch/NumPy wheels. Update pins through a tested PR.
numpy>=2.5.2,<3
pandas==3.0.5
torch>=2.13.0,<3
einops==0.8.2
huggingface_hub==1.29.0
matplotlib==3.11.1
tqdm==4.70.0
safetensors==0.8.0
pytest>=9.1.1,<10
pyyaml>=6.0.3,<7
