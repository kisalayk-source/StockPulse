# Lag-Llama — install in an isolated env; GluonTS stack can conflict with other TS libs.
# See https://github.com/time-series-foundation-models/lag-llama for current install notes.
torch>=2.14.0
gluonts>=0.17.0
numpy>=2.5.3
pandas>=3.0.5
# lag-llama is typically installed from git:
#   pip install git+https://github.com/time-series-foundation-models/lag-llama.git
